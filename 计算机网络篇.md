从你输入url到页面显示中间发生了什么
---

首先我们只知道url，但是不知道IP地址，所以必须对域名进行dns解析，dns解析分为递归查询和迭代查询，递归查询现在基本不使用，迭代查询，则是先本地域名服务器查询有没有
缓存，如果没有，就向根域名服务器查询，如果再没有，就向顶级域名服务器查询

获得IP地址后，就开始发送http请求，http请求包括三次握手，服务器收到请求后，吧页面发给浏览器

浏览器获得页面后，开始对页面进行渲染，这部分需要利用浏览器内核，内核包括js引擎
